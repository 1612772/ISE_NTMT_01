<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.nguyenhuutu.convenientmenu">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name="android.support.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.NoActionBar"
        tools:ignore="GoogleAppIndexingWarning">
        <!--android:theme="@style/Theme.AppCompat.Light">-->
        <activity android:name=".homepage.HomePage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Restaurant_Detail" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ViewDish" />
        <activity
            android:name=".register.SwitchRegister"
            android:label="Đăng ký"></activity>
        <activity
            android:name=".register.CustomerRegister"
            android:label="Đăng ký - Thực khách"></activity>
    </application>

</manifest>